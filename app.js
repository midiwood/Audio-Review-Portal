/**
 * cPanel default startup file.
 * Replace the host's "It works!" app.js with this file (or set startup file to server.js).
 * After upload: npm install && npm run build && Restart App
 */
require("./server.js");
