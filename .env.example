AUTH_SECRET=change-me-to-a-long-random-string-32+
OWNER_EMAIL=owner@studio.local
OWNER_PASSWORD=changeme

# Public app URL used in notification email links
APP_URL=http://localhost:3000

# Optional email via Resend (notifications still work in-app without these)
RESEND_API_KEY=
EMAIL_FROM=Audio Review <onboarding@resend.dev>
